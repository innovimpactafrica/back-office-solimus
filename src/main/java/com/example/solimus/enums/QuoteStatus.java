package com.example.solimus.enums;

public enum QuoteStatus {
    DRAFT,      // Brouillon (non visible par le syndic)
    SENT,       // Envoyé au syndic (en attente)
    ACCEPTED,   // Validé
    REJECTED    // Refusé
}
